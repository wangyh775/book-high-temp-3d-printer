# 智能体行为规范 (AGENTS.md)

本文件是本项目所有辅助编写、审阅、绘图与重构智能体（AI Agent）的**强制行为约束**。

---

## 1. 操作范围与目录隔离（防冲突红线）

* **单章自治**：智能体**仅在用户明确指定的单章目录**（如 `chapters/XX-xxx/`）内进行修改与新增，严禁擅自跨章节作业。
* **资产就地存放**：
  * 图片与绘图文件必须放入该章节的 `figures/` 子目录；
  * 参考文献条目必须追加至该章节的 `references.bib`。
* **全局写保护（未经显式指示严禁修改）**：
  * 根目录配置文件：`_quarto.yml`、`_brand.yml`、`custom.scss`；
  * 根目录入口文件与文献：`index.qmd`、根目录 `references.bib`；
  * 其它章节目录及扩展模板目录 `_extensions/`。
* **最小修改原则（非章节内容改动红线）**：
  * 当用户明确指示修改全局配置、CI 工作流、扩展模板或文档入口（如 `_quarto.yml`、`_brand.yml`、`.github/workflows/`、`README.md`、`index.qmd` 等非单章内容）时，**必须严格遵从最小修改原则**；
  * 以**最小的代码变动量、最精准的局部增量**来实现目标功能，严禁无故大面积重构、批量格式化无关字段或改动无关逻辑；
  * 严格保持原有配置结构、注释说明与缩进风格的稳定性，杜绝变更范围扩散。

---

## 2. 格式与技术栈规范（防编译崩溃）

本书通过 Quarto 编译为 **Typst PDF** 与 **HTML**，智能体生成内容必须严格保证双端兼容：

* **文件格式**：一律采用 Quarto Markdown（`.qmd`）。
* **严禁原生宏污染**：
  * 严禁在正文中书写原生 LaTeX 文本宏（如 `\textbf{}`、`\cite{}`、`\ref{}` 等）；
  * 严禁手写裸 HTML 标签（如 `<div>`、`<span>`、`<br>` 等）。
* **数学公式**：
  * 行内公式使用 `$ ... $`，独立公式块使用 `$$ ... $$`；
  * 独立公式必须在末尾标注引用标签 `{#eq-xxx}`，正文统一使用 `@eq-xxx` 引用。
* **图表与交叉引用**：
  * 矢量图优先（`.svg`、`.pdf`），实物照片使用高分辨率 `.png`，严禁使用外部网络图床；
  * 图片语法：`![图片描述](figures/name.svg){#fig-name}`，正文引用 `@fig-name`；
  * 表格语法：必须在表尾指定 `: 表格标题 {#tbl-name}`，正文引用 `@tbl-name`；
  * 章节引用：在标题后附加 `{#sec-name}`，正文引用 `@sec-name`。
* **Callout 语法**：必须使用 Quarto 原生块语法（`::: {.callout-note}`、`::: {.callout-warning}`、`::: {.callout-tip}` 等）。
* **代码块**：必须显式标注语言类型（如 ```` ```python ````、```` ```cpp ````、```` ```gcode ````），禁止无语言高亮的代码块。
* **纯净章节排版（防 PDF 视觉污染）**：
  * **严禁手动编码标题序号**：全书已配置 `number-sections: true`，各级标题（`##`、`###`）会自动计算生成章节序号（如 1.1, 1.2, 1.2.1）。**严禁在 Markdown 标题内手写编号**（如 `## 1.1 标题`、`### 1.2.1 标题`、`### 一、选择题`），否则会导致 Typst PDF 和网页端出现 `1.1 1.1 标题` 的重复编号。所有章节标题必须直接写纯文字；
  * **严禁在章节开头添加冗余导读块**：严禁在章节正文开头添加类似“本章导读”、“学习目标”、“单章 PDF 下载链接”等提示框（Callout），这些导航与链接已在全书目录与 GitHub 主页集中展示，正文开头添加会导致导出的章节 PDF 被严重视觉污染。正文开头必须直接进入技术小节。

---

## 3. 全书工程参数基线（防数据冲突）

各章节凡涉及整机参数、工况计算或元器件选型，必须统一对齐以下基准，禁止自造矛盾参数：

| 系统分项 | 规范基准参数 | 适用说明 |
| :--- | :--- | :--- |
| **喷嘴极限温度** | $\ge 450^\circ\text{C}\sim 500^\circ\text{C}$ | 匹配高熔点特种塑料，标配全金属/硬质合金/金刚石喷嘴 |
| **恒温热室温区** | $90^\circ\text{C}\sim 150^\circ\text{C}$ | 针对 PEEK（$T_g \approx 143^\circ\text{C}$）与 PEI 在线应力退火与结晶控制 |
| **成型热床温度** | $120^\circ\text{C}\sim 200^\circ\text{C}$ | 提供首层高附着力，防止收缩撕脱 |
| **外壳安全温度** | $\le 55^\circ\text{C}$ | 符合工业设备接触安全标准（GB/T 8464） |
| **基准材料体系** | PEEK、PEKK、PEI (ULTEM)、PPS | 兼顾纯树脂与碳纤/玻纤增强复合耗材 |

---

## 4. 内容与文献红线

* **严禁伪造文献**：追加至 `references.bib` 的文献必须真实可查（具备真实作者、发表年份、刊名或 DOI）。无法确认真实来源时，改用通用工程经验表述或标记 `<!-- TODO: 补充文献 -->`，严禁自行捏造 BibTeX。
* **去无意义套话**：严禁出现“值得注意的是”、“显而易见”、“双刃剑”、“在这个飞速发展的时代”等无信息填料，直奔物理因果与工程数据。

---

## 5. 交付汇报规范

任务执行完毕后，智能体必须在最终回复中清晰列出：

1. 本次修改/新增的文件路径；
2. 新增的交叉引用标签（`@sec-`、`@fig-`、`@tbl-`、`@eq-`）；
3. 新增文献的 BibTeX Key 及真实性核对情况。
